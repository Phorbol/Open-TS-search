I will reorganize the project structure to follow the standard `src` layout with a namespace package `open_ts_search`. This will make the project installable and prevent name conflicts.

### 1. Create Directory Structure
- Create `src/open_ts_search` directory.
- Create an empty `src/open_ts_search/__init__.py` to mark it as a package.

### 2. Move Core Packages
Move the following directories into `src/open_ts_search/`:
- `algo`
- `core`
- `registry`
- `shared`

### 3. Update `setup.py`
Modify `setup.py` to:
- Use `package_dir={'': 'src'}` to tell setuptools that packages are under `src`.
- Use `find_packages(where='src')` to locate the packages.

### 4. Refactor Imports
Update all import statements in the codebase (including `src`, `examples`, and `tests`) to use the new namespace:
- `from algo` → `from open_ts_search.algo`
- `from core` → `from open_ts_search.core`
- `from registry` → `from open_ts_search.registry`
- `from shared` → `from open_ts_search.shared`
(And similarly for `import` statements)

### 5. Cleanup & Verification
- Remove old `open_ts_search.egg-info` directory.
- Install the package in editable mode (`pip install -e .`).
- Run a verification script to ensure `open_ts_search` can be imported and submodules are accessible.